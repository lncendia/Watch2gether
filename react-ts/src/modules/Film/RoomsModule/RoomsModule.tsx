import {Film} from "../../../services/FilmsService/Models/Film.ts";

const RoomsModule = ({film, className}: { film: Film, className?: string }) => {
    return (
        <div>

        </div>
    );
};

export default RoomsModule;